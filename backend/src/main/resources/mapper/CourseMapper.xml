<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ynu.mapper.CourseMapper">
    <select id="selectByPage" resultType="com.ynu.pojo.entity.Course">
        select course_id,title,category_name,introduction,price,start_date,end_date,max_participants,cover_image from course
    </select>
</mapper>